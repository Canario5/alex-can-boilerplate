{
  "name": "alex-can-boilerplate",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "dev",
  "runServices": ["dev"],
  "workspaceFolder": "/app",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "corepack prepare \"$(node -p \"require('./package.json').packageManager\")\" --activate && pnpm install --frozen-lockfile && pnpm exec playwright install chromium",
  "containerEnv": {
    "LEFTHOOK_CONFIG": "lefthook-devcontainer.yml"
  },
  "features": {},
  "customizations": {
    "jetbrains": {
      "plugins": ["com.github.biomejs.intellijbiome"]
    },
    "vscode": {
      "extensions": [
        "biomejs.biome",
        "Codecov.codecov",
        "ms-playwright.playwright",
        "vitest.explorer",
        "github.vscode-github-actions",
        "coderabbit.coderabbit-vscode"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
          "source.fixAll.biome": "explicit",
          "source.organizeImports.biome": "explicit"
        }
      }
    }
  }
}
